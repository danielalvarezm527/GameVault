import React from "react";
import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const GamingNewsScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Gaming News</Text>
      </View>
    </SafeAreaView>
  );
};

export default GamingNewsScreen;
